{
  "name": "@testdata-generator/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "td": "./dist/td.js"
  },
  "scripts": {
    "build": "bun build ./bin/td.ts --outdir ./dist --target bun --format esm",
    "test": "bun test",
    "test:bdd": "bun run build && bun ./tests/run-cucumber.ts"
  },
  "dependencies": {
    "@testdata-generator/core": "workspace:*",
    "chalk": "^5.6.2",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.5.0",
    "@types/bun": "^1.0.0"
  }
}